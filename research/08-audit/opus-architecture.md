# opus 적대적 아키텍처 감사 — video-factory 설계 v1

- 감사일: 2026-07-07 · 렌즈: 5계층 아키텍처 + 실행 시퀀스의 구조 결함
- 대상: MASTER-PLAN.md(§2 5계층, §3 Phase, §5 시퀀스) · engine-internals.md · skill-pack.md · goal-and-constraints.md · dashboard-edit-loop.md
- 원칙: 문서에서 직접 확인된 모순만 기재. 칭찬 없음.

| 번호 | 심각도 | 대상(문서:섹션) | 결함 | 수정 제안 |
|---|---|---|---|---|
| 1 | 치명 | MASTER-PLAN §2.1/§2.4 render-manifest "씬 duration 없음, 오디오 실측이 유일 권위" + skill-pack §6.3-3 "루트 data-duration 컴파일타임 고정" | **나레이션 수정 → duration 변화 → 후속 씬 전부 시프트 문제를 설계가 다루지 않는다.** data-start가 누적합/상대참조(§6.2)이고 루트 duration은 변수/스크립트로 못 바꿈. 사용자가 씬3 나레이션 1글자 고침 → TTS 실길이 변화 → 씬3 슬롯 duration 변화 → 씬4~N data-start 전부 밀림 + 루트 duration 변화 → BGM 더킹 엔벨로프·전환 겹침창 전부 무효 → **가장 흔한 편집(나레이션 텍스트)이 항상 전체 재컴파일+전체 재렌더를 강제**. §2.4의 "TTS 재생성만 씬 스코프 버튼" 주장은 거짓(실효는 전역 시프트). dashboard-edit-loop의 "무렌더 프리뷰" 교훈은 Remotion Player가 duration을 런타임 동적 계산하기에 성립한 것 — 루트 duration을 컴파일타임 동결하는 hyperframes엔 이식 불가. | 씬 슬롯을 절대 data-start 누적합이 아니라 씬간 gap 토큰 + 컴파일러가 매 편집마다 전 씬 재타이밍하는 것을 기본 전제로 명문화. "나레이션 편집=씬 로컬"이라는 UX 약속 철회, 편집 UI에 "이 편집은 전체 재렌더 필요" 배지 도입. P0에서 반드시 실증. |
| 2 | 치명 | MASTER-PLAN §2.4 "씬만 재렌더" vs skill-pack §6.2/engine-internals §7(c) "최종본은 메인 전체 렌더 1회, 씬 concat은 초안용" | **씬=서브컴포지션 단독 --composition 재렌더는 전환도 BGM도 없는 반쪽 산출물이다.** 전환은 메인 타임라인에 from연장+to당김+트랙핑퐁으로 두 씬을 걸쳐 주입되고(§3.4), BGM은 index.html 루트 직계 `<audio>`다. 씬 파일 하나만 render -c 하면 진입 전환·이탈 핸드오프·배경음악이 전부 빠진 클립이 나온다. "확정 시 해당 씬만 재렌더"(§2.4 핵심 약속)가 컴파일러 자체 아키텍처와 모순. 재통합 경로(씬 mp4를 메인에 어떻게 되꽂나) 미정의. | 편집 루프 산출을 2티어로: 프리뷰/초안=씬 단독 render -c(전환·BGM 없음 명시), 확정본=메인 전체 재렌더만 인정. "부분 재렌더=최종품질" 마케팅 문구 삭제. |
| 3 | 치명 | MASTER-PLAN §2 "층간 직접호출 금지, 파일계약만" + §2.4 편집대상=scene_specs vs engine-internals §7(a) "편집은 PUT /files/* 또는 file-mutations/*로 컴포지션 소스 변형" | **L2 컴파일러와 L3 스튜디오가 같은 파일을 반대 방향으로 쓴다(lost update).** 재사용하는 Studio 서버 API는 컴포지션 HTML을 진실원천으로 뮤테이트하도록 설계됨(data-hf-id 기반 file-mutations). 그러나 계획은 scene_specs.json을 진실원천으로 두고 컴포지션을 파생 산출물로 취급 → 재컴파일이 컴포지션 HTML을 덮어씀. Studio UI/자체패널에서 컴포지션을 직접 편집한 값은 다음 재컴파일에서 **조용히 소실**. 두 진실원천이 동일 아티팩트를 양방향 점유. | 컴포지션 HTML을 read-only 빌드산출물로 계약 고정. Studio의 파일쓰기 라우트(PUT/files, file-mutations, gsap-mutations)를 대시보드에서 차단(프록시 화이트리스트 read-only). 모든 편집은 scene_specs 패치→재컴파일 단일 경로만. |
| 4 | 치명 | MASTER-PLAN §1 결정7/§4-7 "html-video data-hv-text 안정키로 재렌더 없는 인라인 텍스트 편집 이식" | **이식하려는 기능의 전제가 새 엔진에서 무너진다.** §1 스스로 html-video 엔진을 "이름뿐, Playwright 화면녹화, 결정론 아님"이라 판정했다. data-hv-text의 "재렌더 없는 텍스트 편집"은 바로 그 Playwright-DOM-라이브 모델에서만 성립 — hyperframes는 결정론 프레임캡처라 텍스트가 프레임에 베이크되어 어떤 텍스트 변경도 픽셀 재렌더가 필수(determinism-rules "같은 t→같은 픽셀"). 비결정론 모델의 기능을 결정론 엔진에 이식하면 "재렌더 없음" 약속이 거짓이 됨. 게다가 data-hv-text와 hyperframes 자체 data-hf-id 안정ID 체계가 같은 DOM에 공존해 Studio 셀렉션/patch API가 어느 키를 앵커로 쓰는지 충돌. | data-hv-text 이식 폐기(전제 불성립). 인라인 텍스트 편집=scene_specs 필드 패치→씬 재컴파일→씬 render로 단일화. 안정키는 hyperframes data-hf-id 하나만 사용. |
| 5 | 치명 | engine-internals §0/§7 마지막줄 "벤더 0.7.26 vs npm 0.7.37, 변경 미확인, 라우트 재확인 필요" + §5 "~/.hyperframes auto-update.log" | **L3 전체가 사설·자동갱신 API의 미검증 시그니처 위에 서 있다.** §2.4가 하드코딩 재사용하는 라우트(file-mutations, /preview/comp/*, /thumbnail, SSE /api/events, render body 스키마)는 오직 0.7.26에서만 확인됨. 설치본은 auto-update로 버전이 고정도 안 됨. 어느 날 hyperframes가 0.7.4x로 자동 상향되어 라우트 시그니처가 바뀌면 스튜디오가 조용히 깨짐. 계획에 버전핀·라우트 계약테스트·어댑터 층이 없음. | P4 착수 전 hyperframes 버전 핀(package.json exact + auto-update off) + 재사용 라우트 전부에 대한 계약 스냅샷 테스트(응답 스키마 골든) 추가. Studio API를 직접 부르지 말고 얇은 어댑터 모듈 1곳으로 격리해 버전업 브레이크를 한 파일에 국한. |
| 6 | 치명 | MASTER-PLAN §3 P0 PoC / §5 T2 게이트 범위 | **PoC가 설계를 무효화할 실제 리스크를 안 건드린다.** T2는 3씬→15초 MP4 관통 + 씬1개 단독 재렌더 + Studio iframe만 실증 — 즉 해피패스만 증명. 결함1(나레이션→시프트→전체재렌더), 결함2(씬 단독 재렌더의 전환·BGM 결손 재통합), 결함5(버전드리프트), 한국어TTS+CJK폰트 렌더는 전부 PoC 밖. "씬1개 단독 재렌더 실증"은 메커니즘 존재만 증명하고 사용가능성(전환/BGM 포함)은 증명 안 함. 리스크 제거용 게이트가 엉뚱한 걸 검증. | PoC 게이트에 필수 시나리오 추가: (a) 씬2 나레이션 편집→재컴파일→후속 씬 시프트가 전환/BGM 정합 유지하는지, (b) 편집된 씬 재렌더분이 메인 최종본에 이음매 없이 재통합되는지, (c) 한국어 TTS+.woff2 임베드 실렌더, (d) 벤더 갱신 후 라우트 생존. 이 4개 실패 시 설계 회귀. |
| 7 | 중대 | MASTER-PLAN §2.4 "프리뷰=최종렌더 같은 HTML, 부분재렌더가 런타임 재실행으로 환원(auto_kairos 교훈)" vs engine-internals §1 "헤드리스 크롬은 video/audio 재생 불가, 오디오는 렌더타임 ffmpeg 믹스만" | **프리뷰 iframe엔 오디오가 없다 — WYSIWYG 주장이 시각에만 참.** auto_kairos WYSIWYG은 @remotion/player가 라이브 오디오까지 같은 트리로 재생해 성립한 것. hyperframes GET /preview/comp/*.html 은 헤드리스가 오디오를 못 울리므로 프리뷰에서 나레이션·BGM·더킹·워드싱크 자막 타이밍(G-V2)을 검증 불가. "런타임 재실행으로 환원" 교훈이 오디오 축에서 미이식. | 프리뷰를 "무음 시각 확인용"으로 격하 명시. 오디오-자막 정합 검증은 실렌더 산출 + ffprobe/OCR 게이트(G-V2/G-V4)로만 보장한다고 계약. 프리뷰에 오디오 파형 오버레이(waveform API 존재)로 타이밍 근사 표시. |
| 8 | 중대 | MASTER-PLAN §4-4 "자체 바닐라JS 패널, React 금지" + engine-internals §7 "preview가 Studio(React) 자동 오픈, 사용자 직접편집 가능"(hyperframes-cli §5.2) | **재사용하는 preview 서버가 파일쓰기 가능한 React Studio를 자동으로 함께 띄운다 → 이중 편집면, 락 없음.** 계획은 자체 패널만 쓰겠다지만 같은 서버가 Studio UI도 서빙하고 그건 컴포지션 파일을 직접 뮤테이트한다. 두 편집면이 동일 파일에 동시 접근, 조정/락 부재 → 사용자가 Studio에서 만진 값과 자체 패널의 scene_specs 패치가 서로 덮음. | preview를 헤드리스 API 모드로만 기동(Studio UI 오픈 억제 플래그/포트 프록시로 UI 라우트 차단), 또는 Studio를 read-only로 강등. 단일 편집면 불변식 문서화. |
| 9 | 중대 | MASTER-PLAN §2.2 전환 인젝터 + §2.4 "씬 단위 재컴파일" vs transitions/overview "전환=exit, 나가는·들어오는 씬이 같은 T에 동시 애니" | **전환은 인접 두 씬+메인 타임라인을 걸치는 전역 변형이라 씬 로컬 편집이 불가능하다.** 씬 재정렬·삭제·삽입 시 인접 전환 전부 재주입+메인 재스탬프 필요 → 전역 재컴파일. 또 scene_specs가 씬마다 transition 필드를 갖는데 N과 N+1 사이 전환의 소유자가 누구인지(진입 속성? 이탈?) 모호 — overview는 "전환=exit"라 두 씬이 같은 전환을 이중 주장. | 전환을 씬 필드가 아니라 씬간 엣지 객체(transitions[]{from,to,type})로 모델링. 재정렬/삭제 편집은 "전역 재컴파일 필요" 클래스로 분류해 씬 로컬 UX에서 제외. |
| 10 | 중대 | MASTER-PLAN §2.1 scene_specs.imageAsset{source,prompt} + §2.3 "images/* + image-manifest.json, 재생성=버전증가 gen_02 + selected 포인터" | **"어떤 이미지가 선택됐나"의 진실원천이 둘(scene_specs vs image-manifest selected 포인터)로 갈린다.** L3에서 사용자가 이미지 교체 시 scene_specs.imageAsset.source를 쓰는지 image-manifest selected를 쓰는지 미정. 둘이 어긋나면 컴파일러가 어느 걸 꽂을지 비결정. dashboard-edit-loop는 imageAsset deep-merge로 풀었는데 계획엔 그 규칙 없음. | selected 포인터를 image-manifest 단일 소유로 고정, scene_specs.imageAsset은 prompt/placement만 보유(선택 결과는 참조). 편집 저장 시 deep-merge 규칙 명문화. |
| 11 | 중대 | MASTER-PLAN §2.5 G-V1 "동일입력 2회 렌더 해시 일치" vs hyperframes-media BGM/TTS 비결정성 + engine-internals §1 "seedRandomFromFrame 기본 off" | **결정론 게이트가 비결정 자산과 충돌해 항상 실패한다.** BGM retrieve(HeyGen 카탈로그 top-1)·generate(MusicGen), TTS 프로바이더 출력은 실행마다 다를 수 있고 seedRandom은 기본 off라 Math.random 쓰는 블록/전환은 프레임이 안 맞음. 자산을 얼리고 시드를 켜지 않으면 2회 해시가 절대 안 맞음 — 게이트가 정상 산출을 반려. | G-V1을 "자산 동결(오디오/이미지 고정 경로) + seedRandomFromFrame on 조건에서만 2회 해시비교"로 한정. 자산 생성 단계와 렌더 결정론 검증 단계를 분리(생성은 비결정 허용, 검증은 동결본 대상). |
| 12 | 중대 | MASTER-PLAN §4-4 "JSON Schema→폼 자동생성" (근거: skill-pack §1.5 Studio 변수 폼) | **인용한 재사용 근거가 대상 스키마에 적용 안 된다.** Studio 자동 폼생성은 컴포지션의 data-composition-variables 선언을 소비한다(§1.5). 그러나 편집 대상은 별도 스키마인 scene_specs.json이지 컴포지션 변수가 아님 → Studio 변수-폼 메커니즘 재사용 불가, scene_specs JSON Schema용 폼생성기를 새로 만들어야 함. "폼 자동생성의 근거"라 인용된 skill-pack §1.5는 다른 스키마 얘기. | scene_specs → data-composition-variables 브리지를 만들어 실제로 Studio 폼기전을 태우거나, 자체 폼생성기를 신규 구현 대상으로 계획에 명시(재사용 아님을 정정). |
| 13 | 중대 | MASTER-PLAN §2.4 "SSE 핫리로드로 프리뷰 갱신" vs engine-internals §2 "fs.watch(recursive)+SSE, renders/outputs/node_modules 제외" | **재컴파일이 감시 대상 파일을 쓰기 때문에 SSE 리로드 폭주/루프 위험.** 편집→재컴파일이 compositions/* HTML을 덮어씀 → fileWatcher가 file-change 발화 → 리로드. compositions/는 제외 목록에 없음. scene_specs.json도 프로젝트 내면 편집 저장이 즉시 SSE 발화. 오리진 태깅/디바운스 없으면 컴파일-감지-재트리거 진동. | fileWatcher 제외 목록에 compositions/(빌드산출)·scene_specs.json 추가하거나, 재컴파일을 별도 out 디렉토리로 원자적 스왑 후 단일 명시적 리로드 이벤트만 발화. |
| 14 | 중대 | MASTER-PLAN §2.3 TTS/전사 + skill-pack §2.2/§6.4 "Kokoro ko 없음, CJK 폰트 자동임베드 없음" | **한국어 경로가 유료 크리덴셜에 경성 의존하는데 실행 시퀀스가 이를 리스크로 안 다룬다.** 로컬 폴백 Kokoro는 한국어 미지원 → HeyGen/ElevenLabs 키 필수. 키 없으면 한국어 TTS 자체가 불가(무음 폴백도 없음). CJK .woff2 미동봉 시 렌더 머신 클린 크롬에서 자막이 두부(□) 렌더. §5 자동완주 조항은 "fable 한도"만 정지조건으로 두고 크리덴셜 부재를 안 다룸. | P0에 한국어 TTS 크리덴셜 프리플라이트 + .woff2 임베드 실렌더 필수 통과 추가. 키 부재 시 파이프라인 명시적 중단(무음 진행 금지). 폰트 번들링을 컴파일러 하드룰로. |
| 15 | 경미 | MASTER-PLAN §2.1 "오디오 실측 초 단위" + engine-internals §1 "fps 유리수 30000/1001" | **초 단위 오디오 권위와 프레임 유리수 그리드의 반올림 누적.** duration_s(float)를 슬롯 data-duration(초)으로 넣고 data-start를 float 초 누적합으로 쌓으면 NTSC 유리수 fps 프레임 경계와 어긋나 다수 씬에서 오디오 드리프트/마지막 프레임 잘림. G-V2는 endSec ≤ audioDurationSec만 보고 프레임 양자화 드리프트는 안 봄. | 컴파일러가 모든 타이밍을 프레임 인덱스로 양자화(round to frame)한 뒤 초로 역산해 저작. G-V2에 누적 드리프트(마지막 씬 end vs 총 오디오길이 프레임 오차) 검사 추가. |

## 요약

- **치명 6 / 중대 8 / 경미 1** (총 15).
- 근본 결함 한 줄: **"씬 로컬 편집·부분 재렌더"라는 핵심 약속이 hyperframes의 컴파일타임 고정 루트 duration·메인 타임라인 전환·루트 오디오·결정론 프레임캡처와 정면충돌하며(치명 1·2·4), 이를 무효화할 시나리오를 P0 PoC가 검증하지 않는다(치명 6). 재사용한다는 Studio API는 자동갱신 사설 표면(치명 5)이자 컴파일러와 반대 방향으로 같은 파일을 쓰는 lost-update 원천(치명 3)이다.**
