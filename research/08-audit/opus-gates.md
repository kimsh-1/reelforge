# opus-gates — 검증 체계 적대 감사 (게이트 우회·허점)

감사: 2026-07-07 · 대상: VERIFICATION-PLAN.md(L0~L4 38게이트) · WORKERS.md(hooks·불신) · MASTER-PLAN.md(Phase 종료조건). 렌즈: "코드가 나쁜데도 통과"·판정 캘리브레이션 부재·커버리지 구멍·skeptic-hook 위조·L4 배치·Phase 매핑 누락. 칭찬 없음.

| 번호 | 심각도 | 대상 게이트 | 우회/허점 시나리오 | 보강 제안 |
|---|---|---|---|---|
| 1 | 치명 | 운영규칙1 게이트 리포트 계약 · skeptic-hook | 훅이 리포트 파일 **부재만** 검사한다. (a) inputHash는 리포트 안에 적혀 있을 뿐, 훅이 현재 입력에서 해시를 **재산출·대조하지 않으면** 옛 리포트 재사용으로 통과. (b) `pass:false` 리포트도 "존재"하므로 부재검사만으로는 통과. (c) 리포트는 codex가 그냥 write하는 JSON — 게이트 스크립트가 실제 돌았다는 서명·증빙 없음(위조 가능). | 훅이 (i) 현재 입력에서 inputHash 재계산→불일치 시 stale 거부, (ii) `pass===true` 강제, (iii) 게이트 러너만 아는 HMAC/실행 로그 첨부로 provenance 검증. |
| 2 | 치명 | L2-8 opus 3표 · U-4 4/5 | "3표 다수결"·"4/5 이상"의 **루브릭·골든 라벨셋·판정자간 편차 측정이 전무**. 같은 프레임을 배치마다 다르게 채점해도 감지 못 함. P5 "opus 캘리브레이션"은 캘리브레이션 산출물(골든 채점 앵커)을 정의하지 않음 → drift 방치. | 라벨된 골든 스냅샷 세트(합격/불합격 앵커) 고정, 3표 IRR(합치율) 임계 설정, 앵커 재채점을 매 배치 선행해 편차 초과 시 판정 무효화. |
| 3 | 치명 | L1-4·L1-5·L3-1 mock 경로 | 대부분 게이트가 **no-credential mock**에서 돈다. 실 TTS(L3-2)는 P6 1회+청취뿐. mock audio_meta로 L1-3·L2-6·L2-9 자막/총길이/워드싱크를 다 검증하므로 실 HeyGen/ElevenLabs의 실제 길이·인코딩·부분실패·레이트리밋은 **미검증**. 게다가 L2-1 결정론은 실 TTS가 비결정적이라 **오직 mock에서만 통과 가능** — 실경로 결정론은 영구 미측정. | 실 TTS 스모크를 P3부터 게이트화, 실 audio_meta로 L2-6/L2-9 재통과 요구, 결정론은 "오디오 고정 후 렌더단"으로 범위 명시. |
| 4 | 치명 | L2-2 씬 단독 재렌더(타 씬 프레임 해시 불변) | 전환 인젝터가 from연장/to당김으로 **인접 씬 프레임을 의도적으로 커플링**한다. 씬 N 재렌더 시 N±1 전환 프레임은 **정당하게 변한다** → 게이트가 오탐(false-fail)하거나, 전환 프레임을 제외하면 그 구간이 **영구 사각**이 됨. 컴파일러가 전환을 사실상 무효화해도 이 게이트로는 잡히지 않음. | 재렌더 무결성을 "전환 오버랩 프레임 제외 본문 해시 불변 + 전환 프레임은 명세 재산출값과 일치"로 이원화. |
| 5 | 중대 | L2-8·L2-3·L2-4·L2-5 스냅샷 3프레임 | 초입/중간/말미 **고정·기지(旣知) 타임스탬프만** 샘플 → 그 3점만 멀쩡하고 사이 구간에 모션 글리치/자막 오버플로/텍스트 번인이 있어도 통과(프레임 선택 오버피팅). | 랜덤 시드 프레임 추가 샘플 + 자막/OCR/HEX는 전(全) 프레임 또는 조밀 그리드 스캔으로. |
| 6 | 중대 | L1-1·L1-2 골든 픽스처 | L1-1은 골든 spec **3종**뿐, L1-2는 전환 40종 **각 1케이스**. 컴파일러가 이 입력들에만 맞춘 하드코딩/룩업이어도 DOM diff 통과. 미지 spec·전환 조합(다른 duration 간 전환·연속 동일전환)은 미검증. | 프로퍼티/퍼즈 생성 spec 다수, 전환은 duration·트랙 조합 매트릭스로 케이스 확장, 골든 외 무작위 입력 스모크. |
| 7 | 중대 | (부재) 편집 동시성·렌더 중 편집 | L3-3은 **단일 편집**만. 두 편집이 scene_specs.json을 동시 패치(경합·lost update)하거나 **렌더 진행 중 편집**으로 입력이 바뀌는 경로에 게이트가 없음. 대시보드가 조용히 덮어써도 통과. | scene_specs 낙관적 락/버전 토큰, 렌더 중 편집=큐잉/거부 계약, 동시편집 E2E 시나리오 추가. |
| 8 | 중대 | (부재) 긴 영상·메모리 | L3 시나리오 최장 60초. **3분+·씬 20개 장영상의 Chromium OOM/메모리 누수**(WORKERS가 인정한 OOM 경합) 게이트 없음. short-video는 통과하나 실사용 장척에서 붕괴. | 3분+ 장영상 + 고해상 렌더 메모리/완주 게이트를 P5에 추가, 피크 RSS 리포트. |
| 9 | 중대 | L0-3 / (부재) 폰트 임베드 | L0-3은 HEX·대비만 검사. **CJK .woff2 미임베드/폰트 미설치 환경**에서 두부(tofu)로 렌더돼도, OCR(L2-4)은 "글자 아님"으로 0건 통과·bbox(L2-3)도 통과 → 깨진 한글이 final로 감. | 폰트 실제 임베드·글리프 커버리지 게이트, 폰트 제거 환경 렌더 스모크, 렌더 프레임에서 기대 텍스트 OCR **양성** 대조(존재 검증). |
| 10 | 중대 | L2-1 결정론(동일 머신 2회) | "동일 입력 2회"가 **같은 세션·머신**이라 폰트 폴백·타임스탬프·RNG·GPU 차이로 인한 **WSL↔Lambda/타 머신 간 비결정성**을 못 잡음. P6 Lambda 렌더가 로컬과 달라도 통과. | 교차환경(WSL 로컬 vs Lambda) 해시 대조 게이트, 시드/폰트/타임존 고정 계약 명문화. |
| 11 | 중대 | L2-4 원치 않는 텍스트 OCR "0건" | 화면엔 정당한 텍스트(headline·자막·차트 라벨)가 렌더된다. 전(全)프레임 OCR이면 정당 텍스트로 **오탐**, "생성 이미지 영역"만이면 이미지 가장자리/비샘플 프레임의 번인 텍스트를 **놓침**. CJK OCR 자체 오탐/미탐률 높음 → 판정 신뢰 불가. | 마스크(이미지 소스 영역 한정)+기대 텍스트 화이트리스트 차감 후 잔여 검출, CJK OCR 임계·수동 라벨 검증셋으로 OCR 정확도 자체를 캘리브레이션. |
| 12 | 중대 | Phase 매핑 P2={L2 전체} | L2-6(오디오-비디오 총길이)·L2-9(워드싱크)는 **TTS·whisper 파이프라인(P3 구축)에 의존**하는데 P2 종료조건이 "L2 전체"를 요구 → **순서 모순**(P2에 아직 없는 오디오로 게이트 불가). mock으로 억지 통과시키면 실경로 미검증 고착. | P2는 "오디오 비의존 L2(1,2,3,4,5,7)"로 한정, L2-6/8/9는 P3 종료조건으로 이동. |
| 13 | 중대 | L4 U-3 오조작 20종 (P6 only) | 빈 narration·초장문·이모지·0바이트 이미지 등 **강건성/퍼즈 테스트가 P6에서야** 수행 → 스키마·파이프라인 결함을 마지막에 발견해 대규모 회귀. L3-9는 스키마 반려만 커버, 런타임 강건성은 공백. | U-3을 P1(계약)·P3(파이프라인)로 앞당겨 각 층 구축 직후 고의 오조작 스모크. |
| 14 | 중대 | P1={L0 전체} 검증기 자체 정합 | P1이 스키마+**검증기**를 만드는데 "검증기가 나쁜 입력을 실제로 반려하는가"(네거티브 픽스처)는 L3-9로 **P5에서야** 확인 → P1~P4 내내 통과 검증기가 실은 무력해도 모름(validator-of-validator 공백). | P1에 검증기 네거티브 픽스처 스위트(각 규칙당 반드시 실패해야 하는 입력) 동봉·게이트화. |
| 15 | 경미 | P0={L2-1,L2-2,L3-3} L0 부재 | PoC가 수기 scene_specs를 쓰는데 **L0 정적검증이 없어** 스키마 위반 spec으로 PoC가 통과, 잘못된 가정(예: duration 필드 허용)이 코드에 축적될 수 있음. | P0에 L0-1/L0-5 최소형(스키마·duration 부재) 포함. |

## 총평
치명 4 · 중대 10 · 경미 1. 최대 급소는 **① skeptic-hook이 provenance/freshness가 아닌 파일 존재만 강제(리포트 위조·재사용 가능)**와 **② opus 판정의 캘리브레이션·골든 라벨셋 부재**. 구조적으로는 **mock 경로 편중으로 결정론·자막 게이트가 실 TTS에서 사실상 미검증**이며, **동시성·장영상·폰트·교차환경(WSL/Lambda)** 커버리지 구멍과 **L2 게이트의 Phase 순서 모순(오디오 의존 게이트를 오디오 없는 P2에 요구)**이 다음 급소다.
