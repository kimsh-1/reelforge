| 번호 | 심각도(치명/중대/경미) | 대상 | 결함 | 수정 제안 |
|---:|---|---|---|---|
| 1 | 치명 | `engine-internals.md:100`, `Studio render API` | Studio 렌더 API 변수 주입 주장은 허구다. 0.7.26 `studio-server/src/routes/render.ts`는 `fps, quality, format, resolution, composition`만 받고 `StudioApiAdapter.startRender`에도 `variables`가 없다. 실패 시나리오: 대시보드가 `scene_specs`를 변수로 넘겨도 Studio 렌더는 기본값으로 MP4를 만든다. | Studio route와 adapter에 `variables`를 추가해 `createRenderJob({ variables })`로 전달하거나, Studio API 경로에서는 반드시 HTML과 `data-variable-values`를 디스크에 재컴파일하라. |
| 2 | 치명 | `skill-pack.md:127`, 벤더 `skills/hyperframes-media/scripts/audio.mjs:159` | `HYPERFRAMES_TTS_CONCURRENCY` 계약이 0.7.26 벤더에 없다. 실제 코드는 `Promise.all(lines.map(synthLine))`로 전 라인을 동시 실행한다. 실패 시나리오: 20씬 한국어 TTS에서 Kokoro/Whisper 프로세스가 폭주해 WSL 메모리 고갈로 PoC가 랜덤 실패한다. | 벤더 `audio.mjs`에 동시성 제한을 직접 넣거나, 해당 계약이 있는 버전으로 고정하고 20라인 TTS 스트레스 테스트를 P0에 추가하라. |
| 3 | 치명 | `MASTER-PLAN.md:13,68,86`, `engine-internals.md:99` | “씬 단위 재렌더”가 최종 MP4 부분 갱신까지 가능한 것처럼 쓰였지만, 소스 표면은 컴포지션 단독 렌더만 보장한다. 실패 시나리오: 씬 2만 `--composition`으로 다시 렌더한 뒤 최종본에 끼우면 전환, BGM 연속성, 자막 절대시간이 깨진다. | P0 목표를 “씬 단독 프리뷰 렌더”로 낮추거나, frame-range chunk 재조립과 오디오 재믹스까지 별도 러너로 구현하라. |
| 4 | 치명 | `MASTER-PLAN.md:43`, 벤더 `audio.mjs` 계약 | “BGM 더킹은 hyperframes audio.mjs로 구현”은 거짓이다. `audio.mjs` 출력은 `bgm.path, volume` 수준이고 더킹 엔벨로프가 없다. 실패 시나리오: 나레이션 구간에서도 BGM이 0.8로 고정되어 음성이 묻히는데 게이트는 통과한다. | 컴파일러가 보이스 구간 기반 GSAP `volume` keyframe을 직접 생성하고, `audioMixer`의 volume automation 경로로 검증하라. |
| 5 | 중대 | `skill-pack.md:200`, `TRANSITION-REGISTRY.md`, 실제 `product-launch-video/scripts/transitions.mjs:13-21` | 전환 알고리즘 문서와 실제 스크립트가 충돌한다. 문서는 incoming start를 당기라고 하지만 실제 스크립트는 음성/SFX/캡션 동기 때문에 outgoing만 연장하고 incoming start는 유지한다. 실패 시나리오: 문서대로 구현하면 장면 비주얼이 내레이션보다 0.5초 먼저 떠서 싱크가 어긋난다. | 실제 `transitions.mjs`를 기준으로 “outgoing duration만 연장, incoming/audio/caption start 불변”으로 계획을 고쳐라. |
| 6 | 중대 | `P0 PoC`, 벤더 `preflight.ts`, `linuxDeps.ts`, `browser.ts` | 2주 1인 PoC에 환경 선행 작업이 숨겨져 있다. ffmpeg, ffprobe, chrome-headless-shell, WSL 공유 라이브러리는 자동 해결되지 않는다. 실패 시나리오: 첫 렌더가 `FFmpeg not found` 또는 `libnss3.so missing`에서 멈춰 컴파일러 검증이 시작되지 않는다. | T2 첫 단계에 `doctor --json`, `browser ensure`, ffmpeg 설치, Chrome shared-lib 설치를 별도 게이트로 분리하라. |
| 7 | 중대 | `P0 PoC`, 한국어 자막·폰트 | CJK 폰트 임베드가 PoC 필수인데 일정에 별도 작업으로 잡혀 있지 않다. 실패 시나리오: 한국어 자막이 tofu 또는 시스템 fallback으로 렌더되어 OCR·bbox 게이트와 결정론 해시가 흔들린다. | P0 fixture에 Noto Sans KR 등 `.woff2`와 `@font-face`를 포함하고, 폰트 누락 시 렌더를 실패시키는 정적 검사를 추가하라. |
| 8 | 중대 | `renderOrchestrator.ts:1106-1125`, `preview.ts:323-351` | iframe 서브컴포지션 프리뷰와 `--composition` 렌더 전제조건이 다르다. 프리뷰는 파일만 있어도 감싸지만 렌더는 template subcomp가 `index.html`에 `data-composition-src`로 마운트되어야 한다. 실패 시나리오: 씬 HTML만 만든 PoC는 iframe에서 보이나 단독 렌더가 “not mounted from index.html”로 실패한다. | P0 fixture는 모든 씬을 `index.html`에 마운트하고, unmounted scene negative test를 넣어라. |
| 9 | 중대 | `audio.mjs:187-193` | BGM fallback 가정이 불안정하다. `bgm.mode: retrieve`가 명시되고 HeyGen credential이 없으면 로컬 generate로 내려가지 않고 스킵한다. 실패 시나리오: 무자격 WSL PoC에서 BGM 없는 MP4가 생성되고, 계획의 “BGM 더킹” 검증은 실행되지 않는다. | P0에서는 `bgm.mode`를 `auto` 또는 `generate`로 강제하거나 credential preflight를 통과 조건으로 둬라. |
| 10 | 중대 | `tts.mjs:52-70`, `tts/manager.ts:23-33`, `MASTER-PLAN.md:62` | 한국어 TTS 경로가 설계보다 취약하다. Kokoro 지원 언어에 `ko`가 없고, HeyGen non-English 기본 voice 선택은 `lang` 매칭이 아니라 English 우선 로직이다. 실패 시나리오: 한국어 대본이 영어 voice_id로 요청되거나 provider가 throw해서 audio_meta가 빈다. | provider별 한국어 `voice_id`를 필수 설정으로 만들고, P0 전에 1문장 한국어 TTS와 word timestamp smoke test를 통과시켜라. |
| 11 | 중대 | `MASTER-PLAN.md:86` | P0가 최소 단위가 아니다. 15초 MP4, TTS, 자막, 켄번즈, 전환, 씬 단독 렌더, Studio iframe을 한 게이트에 묶어 실패 원인 분리가 안 된다. 실패 시나리오: 자막 워드타임스탬프 실패 때문에 `--composition` 검증까지 막혀 API 검증 결과가 없다. | P0를 `P0a render/env`, `P0b subcomp/render`, `P0c audio/caption`, `P0d Studio iframe`으로 쪼개고 각 단계 산출물을 고정하라. |
| 12 | 중대 | `engine-internals.md:3-4`, 실행 버전 관리 | 감사 근거는 벤더 0.7.26인데 계획은 `npx hyperframes` 사용 가능성을 열어 둔다. npm 최신 0.7.37과 11패치 차이를 미검증 상태로 둔 채 API 안정성을 전제한다. 실패 시나리오: 구현자가 0.7.37을 받아 Studio route나 audio skill 동작이 바뀌어 감사 결과가 재현되지 않는다. | `package.json`과 lockfile에 0.7.26 또는 검증한 커밋을 고정하고, 업그레이드 시 API 대조 스크립트를 다시 돌려라. |

치명 4개, 중대 8개.